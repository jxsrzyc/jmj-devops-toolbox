# ===== 数据库配置 =====
# DB_ENGINE: sqlite | mysql（同一套代码双模式切换）
DB_ENGINE=mysql

# MySQL 连接配置（DB_ENGINE=mysql 时生效）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=your-password
DB_NAME=ops_toolbox

# SQLite 配置（DB_ENGINE=sqlite 时生效）
# DB_PATH=data.db
