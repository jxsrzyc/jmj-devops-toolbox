---
# ConfigMap — 应用配置
apiVersion: v1
kind: ConfigMap
metadata:
  name: lanqi-svc-params-config
  labels:
    app: lanqi-svc-params
data:
  TZ: "Asia/Shanghai"
  DB_PATH: "/app/data/data.db"
  FLASK_ENV: "production"
